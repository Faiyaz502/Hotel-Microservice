spring:
  application:
    name: RatingService
  datasource:
    url: jdbc:postgresql://localhost:5432/Microservice-hotel
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083

